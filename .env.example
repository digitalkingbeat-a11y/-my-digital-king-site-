# Server-only booking webhook used by app/api/booking/route.ts.
# Local setup: copy this file to .env.local and paste your production n8n webhook URL.
# Vercel setup: add the same key/value in Project Settings > Environment Variables.
N8N_BOOKING_WEBHOOK_URL=https://your-n8n-domain.example/webhook/digital-king-booking
